# UEFI

A PowerShell script to query and export Platform Key (PK), Key Exchange Key (KEK), and Allowed Database (DB) certificates from UEFI firmware.

The script can be installed from the PowerShell gallery by running the following command.

`Install-Script -Name Get-UEFICertificate`
